/** Canonical production origin, used for metadata, canonicals, sitemap & robots.
 *  Override with NEXT_PUBLIC_SITE_URL (e.g. preview/custom domains) if needed. */
export const SITE_URL =
  process.env.NEXT_PUBLIC_SITE_URL ?? "https://comedkrankvscollege.vercel.app";
